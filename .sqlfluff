[sqlfluff]
dialect = duckdb

# IMPORTANT → pas dbt globalement
templater = jinja

max_line_length = 120

exclude_paths =
    target/,
    dbt_packages/,
    .venv/

[sqlfluff:templater:dbt]
project_dir = src/etl_ecom/transformation
profiles_dir = /home/djepeno/.dbt

[sqlfluff:rules]