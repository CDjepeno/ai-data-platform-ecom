# base/mcp/mcp-service.yml
apiVersion: v1
kind: Service
metadata:
  name: mcp
  labels:
    app: mcp
spec:
  selector:
    app: mcp
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP
