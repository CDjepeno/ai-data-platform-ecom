apiVersion: v1
kind: Service
metadata:
  name: semantic-layer
  labels:
    app: semantic-layer
    component: ai-inference
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      name: http
  selector:
    app: semantic-layer