apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: preprod-wildcard-tls
  namespace: default
spec:
  secretName: preprod-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "preprod.djepenodev.ink"
    - "*.preprod.djepenodev.ink"
