apiVersion: v1
kind: Service
metadata:
  name: nessie
  labels:
    app: nessie
    component: iceberg-catalog
spec:
  type: ClusterIP
  ports:
    - port: 19120
      targetPort: 19120
      name: http
  selector:
    app: nessie
