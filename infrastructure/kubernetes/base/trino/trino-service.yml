apiVersion: v1
kind: Service
metadata:
  name: trino
  labels:
    app: trino
    component: sql-engine
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: trino
