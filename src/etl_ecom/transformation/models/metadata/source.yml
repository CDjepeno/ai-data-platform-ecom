version: 2

sources:
  - name: metadata
    description: Technical tables for ETL observability

    schema: metadata

    tables:

      - name: etl_runs
        description: History of ETL runs

      - name: etl_step_runs
        description: History of ETL steps

      - name: etl_metrics
        description: ETL execution metrics

      - name: etl_watermark
        description: Watermarks for incremental loads
